---
phase: 1003-pro-video-prompt-engineering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/product_studio/config.py
autonomous: true
requirements:
  - REQ-PPE-01
  - REQ-PPE-02
  - REQ-PPE-03
  - REQ-PPE-05
  - REQ-PPE-06
  - REQ-PPE-07
  - REQ-PPE-08
  - REQ-PPE-11
  - REQ-PPE-12
  - REQ-PPE-13
must_haves:
  truths:
    - "Every SHOT_PLAN prompt is 400-500 chars with camera-as-sentence, physics micro-details, lens+DOF, color grade, and stability suffix"
    - "10 categories exist with full CATEGORY_CONFIGS (7 original + 3 new)"
    - "Food categories have 4-5 shots, non-food categories have 3 shots"
    - "Each category has domain-specific negative prompts and element/prop library"
    - "Template variables {product}, {hero_action}, {atmosphere}, {micro_detail} present in SHOT_PLANS"
    - "Per-shot-type stability suffixes replace global VIDEO_QUALITY_SUFFIX usage pattern"
  artifacts:
    - path: "src/product_studio/config.py"
      provides: "All prompt constants, category configs, shot plans, negatives, elements"
      contains: "SHOT_PLANS"
  key_links:
    - from: "src/product_studio/config.py"
      to: "src/product_studio/prompt_builder.py"
      via: "imports CATEGORY_CONFIGS, SHOT_PLANS, NEGATIVE_PROMPTS_SHOT_TYPE, NEGATIVE_PROMPTS_BASE"
      pattern: "from src.product_studio.config import"
---

<objective>
Rewrite config.py with Skool-level professional prompt templates, 3 new categories, per-category element libraries, template variables, color grade identity strings, per-shot-type stability suffixes, and consolidated negative prompt system.

Purpose: config.py is the single source of truth for all prompt content. This plan rewrites every prompt to professional commercial standards and adds all new data structures that Plans 02 and 03 depend on.
Output: Fully rewritten config.py with 10 categories, 30-50 shot plan prompts at 400-500 chars each, negative prompt dicts, element libraries, few-shot examples, and color grade strings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

### Prompt Engineering References (MUST read before writing prompts)
@~/.claude/projects/-Users-luigivivian-meme-lab/memory/reference_skool_video_prompts.md
@~/.claude/projects/-Users-luigivivian-meme-lab/memory/reference_skool_prompt_techniques.md
@~/.claude/projects/-Users-luigivivian-meme-lab/memory/reference_image_prompt_skill.md

### Existing Implementation
@src/product_studio/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite CATEGORY_CONFIGS with color grades, elements, template vars, and new categories</name>
  <files>src/product_studio/config.py</files>
  <read_first>
    - src/product_studio/config.py (current state — preserve non-prompt constants like ADS_ENABLED, MUSIC_MAP, TEXT_LAYOUTS, etc.)
    - ~/.claude/projects/-Users-luigivivian-meme-lab/memory/reference_skool_prompt_techniques.md (color grade as identity, atmosphere as depth)
    - ~/.claude/projects/-Users-luigivivian-meme-lab/memory/reference_image_prompt_skill.md (surface, lighting, lens vocabulary)
  </read_first>
  <action>
Rewrite the CATEGORY_CONFIGS dict in config.py. Keep ALL non-prompt constants unchanged (lines 1-69: ADS_ENABLED through STYLE_VIDEO_MODEL). Rewrite CATEGORY_CONFIGS starting at the existing location.

For each of the 7 existing categories (food_cookies, food_chocolate, food_burger, beauty_skincare, fashion_shoes, tech_electronics, beverage), ADD these new keys while keeping existing keys:
- "color_grade": a repeatable color grade identity string (per D-27). Examples:
  - food_cookies: "warm earth tones, slight desaturation, golden highlights"
  - food_chocolate: "deep moody contrast, desaturated shadows, rich golden specular highlights"
  - food_burger: "bold saturated warm tones, high contrast, slight orange push"
  - beauty_skincare: "clean muted warm tones, soft highlight bloom, subtle pink undertones"
  - fashion_shoes: "cool desaturated tones, deep blacks, faint teal undertones"
  - tech_electronics: "cool neutral grade, slight blue-teal shift, clean whites"
  - beverage: "vibrant saturated colors, teal-blue shadows, warm golden highlights"
- "elements": list of 4-6 text-based prompt fragments for environmental props (per D-29/D-30). Examples:
  - food_cookies: ["scattered chocolate chips catching light", "cinnamon sticks and vanilla pods", "fine sugar dust particles floating in backlight", "milk splash frozen mid-pour"]
  - food_chocolate: ["cocoa powder dusting in slow motion", "gold leaf fragments", "dark chocolate shavings", "glossy ganache drip"]
  - food_burger: ["scattered sesame seeds", "fresh lettuce leaves with water droplets", "pickles and onion rings", "ketchup drizzle frozen mid-pour"]
  - beauty_skincare: ["water droplets on marble", "flower petals scattered softly", "silk fabric draped in background", "light refracting through clear liquid"]
  - fashion_shoes: ["fine dust particles floating in backlight", "scattered fabric threads", "concrete texture fragments", "rain droplets frozen mid-air"]
  - tech_electronics: ["subtle light rays through smoke", "circuit board fragments as props", "fiber optic light strands", "reflective glass prisms"]
  - beverage: ["ice cubes with internal fractures catching light", "citrus slices with visible juice droplets", "condensation droplets forming", "fresh mint leaves with water beads"]
- "atmospheres": list of 3-4 atmosphere descriptions for random selection (per D-26). Examples:
  - food_cookies: ["warm kitchen glow with floating flour particles", "cozy bakery atmosphere with steam wisps", "rustic farmhouse warmth with golden light rays"]
  - (similar pattern for each category — atmospheric descriptors that add depth)
- "micro_details": list of 3-4 physics micro-detail descriptions for random selection (per D-02/D-26). Examples:
  - food_cookies: ["crumbs scattering in ultra slow-motion catching backlight", "chocolate chips with visible melting edge", "steam wisps curling through warm side light"]
  - food_chocolate: ["glossy surface with micro-reflections shifting", "melted chocolate with viscous slow flow", "cocoa dust particles settling in beam of light"]
  - food_burger: ["melted cheese gently jiggles and slightly stretches at edges", "small grease droplets forming on patty surface", "steam rising through side light beam"]
  - beauty_skincare: ["water droplets slowly forming on cool surface", "light caustics dancing through translucent liquid", "micro-bubbles rising in serum"]
  - fashion_shoes: ["dust particles floating through rim light beam", "subtle fabric fiber movement in air current", "micro-scratches on sole catching specular light"]
  - tech_electronics: ["subtle screen glow pulsing with color shift", "micro-reflections on polished metal edges", "faint electromagnetic interference pattern in background"]
  - beverage: ["condensation droplets slowly sliding down glass", "micro-bubbles rising in carbonated liquid", "ice cracking with internal fracture lines spreading"]

Add 3 NEW categories (per D-05):
- "jewelry_watches": display_name "Joias & Relogios", surface "black velvet | polished obsidian surface", lighting "single focused spotlight from above creating dramatic specular highlights on metal", mood "luxurious, precise, aspirational", lens "100mm macro, f/2.8, extreme shallow DOF", hero_actions: ["watch crown rotating with visible mechanism", "gemstone catching light with prismatic refraction", "clasp opening in ultra slow-motion"], video_camera_moves: ["ultra slow orbit", "macro push-in to dial", "static with light sweep"], negative: "cheap appearance, tarnished metal, flat lighting, text, watermark", color_grade: "high contrast, deep blacks, warm golden specular highlights, desaturated midtones", elements: ["crushed velvet folds catching light", "diamond dust particles floating", "reflective surface creating mirror image", "gold chain links as background texture"], atmospheres: [...], micro_details: [...]
- "candles_scented": display_name "Velas & Aromas", surface "natural linen | aged wood | stone surface", lighting "warm candlelight glow as primary light source with gentle fill", mood "tranquil, aromatic, intimate", lens "85mm, f/2.8, soft dreamy bokeh", hero_actions: ["flame flickering gently with wax pool ripple", "smoke wisps curling after extinguish", "wax melting and pooling in slow-motion"], video_camera_moves: ["static with gentle flame flicker", "slow push-in to flame", "dolly out revealing arrangement"], negative: "harsh lighting, clinical feel, cold colors, text, watermark", color_grade: "warm amber tones, soft shadows, gentle highlight bloom, slight desaturation", elements: [...], atmospheres: [...], micro_details: [...]
- "supplements_bottles": display_name "Suplementos & Vitaminas", surface "clean white surface | marble counter | gym environment", lighting "bright clean key light with subtle rim separation, health-focused clean whites", mood "clean, energetic, trustworthy", lens "85mm, f/4, sharp product focus", hero_actions: ["capsules pouring from bottle in slow-motion", "powder mixing into liquid with swirl patterns", "bottle rotating to reveal label details"], video_camera_moves: ["slow orbit", "push-in to label", "dolly out with lifestyle context"], negative: "medical/clinical, dark mood, untrustworthy feel, text, watermark", color_grade: "bright clean whites, vibrant accent colors, high key, minimal shadows", elements: [...], atmospheres: [...], micro_details: [...]

Also update CATEGORY_DEFAULT to include the new keys (color_grade, elements, atmospheres, micro_details) with generic fallback values.

Add new constants after CATEGORY_DEFAULT:

STABILITY_SUFFIXES dict (per D-03) — replace global VIDEO_QUALITY_SUFFIX pattern:
```python
STABILITY_SUFFIXES: dict[str, str] = {
    "static_macro": "Locked camera, no movement, no jitter, no drift, no deformation. Ultra slow motion, stable picture.",
    "dolly_out": "Smooth controlled dolly movement, no jitter, no drift, no deformation. Stable tracking, smooth motion.",
    "dolly_in": "Smooth controlled dolly push-in, no jitter, no drift, no deformation. Stable tracking, smooth motion.",
    "push_in": "Controlled dolly push-in, steady tracking, no jitter, no drift, no deformation. Stable picture.",
    "orbit": "Smooth orbital movement, no jitter, no wobble, no drift, no deformation. Controlled rotation, stable tracking.",
    "tilt_up": "Smooth controlled tilt, no jitter, no drift, no deformation. Stable vertical movement.",
    "pull_back": "Smooth controlled pull-back, no jitter, no drift, no deformation. Stable tracking.",
    "product_rotate": "Smooth product rotation, no jitter, no wobble, no deformation. Stable picture, controlled spin.",
    "crane": "Smooth crane sweep, no jitter, no drift, no deformation. Controlled vertical movement, stable picture.",
}
STABILITY_SUFFIX_DEFAULT = "Smooth motion, no jitter, no drift, no deformation. Stable picture, ultra slow motion."
```

NEGATIVE_PROMPTS_BASE (per D-08/D-12) — global base negative:
```python
NEGATIVE_PROMPTS_BASE = "blurry, low quality, distorted, overexposed, underexposed, noise, grain, artifacts"
```

NEGATIVE_PROMPTS_SHOT_TYPE dict (per D-09/D-12):
```python
NEGATIVE_PROMPTS_SHOT_TYPE: dict[str, str] = {
    "static_macro": "out of focus subject, camera shake, movement blur",
    "dolly_out": "jerky movement, speed changes, unstable tracking",
    "dolly_in": "jerky movement, speed changes, unstable tracking",
    "push_in": "jerky movement, speed changes, unstable tracking, overshoot",
    "orbit": "wobbly rotation, uneven speed, axis drift, stuttering",
    "tilt_up": "jerky vertical movement, speed changes, horizon drift",
    "pull_back": "jerky movement, speed changes, unstable pull",
    "product_rotate": "wobbly rotation, uneven spin, axis wobble",
    "crane": "jerky vertical sweep, unstable arc, speed changes",
}
```

Keep VIDEO_QUALITY_SUFFIX for backward compat but add a deprecation comment pointing to STABILITY_SUFFIXES.
  </action>
  <verify>
    <automated>cd /Users/luigivivian/meme-lab && python -c "
from src.product_studio.config import CATEGORY_CONFIGS, CATEGORY_DEFAULT, STABILITY_SUFFIXES, STABILITY_SUFFIX_DEFAULT, NEGATIVE_PROMPTS_BASE, NEGATIVE_PROMPTS_SHOT_TYPE
cats = list(CATEGORY_CONFIGS.keys())
assert len(cats) == 10, f'Expected 10 categories, got {len(cats)}: {cats}'
for name in ['jewelry_watches', 'candles_scented', 'supplements_bottles']:
    assert name in cats, f'Missing new category: {name}'
for name, cfg in CATEGORY_CONFIGS.items():
    for key in ['color_grade', 'elements', 'atmospheres', 'micro_details']:
        assert key in cfg, f'{name} missing {key}'
    assert len(cfg['elements']) >= 4, f'{name} elements < 4'
    assert len(cfg['atmospheres']) >= 3, f'{name} atmospheres < 3'
    assert len(cfg['micro_details']) >= 3, f'{name} micro_details < 3'
for key in ['color_grade', 'elements', 'atmospheres', 'micro_details']:
    assert key in CATEGORY_DEFAULT, f'CATEGORY_DEFAULT missing {key}'
assert len(STABILITY_SUFFIXES) >= 8, f'Only {len(STABILITY_SUFFIXES)} stability suffixes'
assert len(NEGATIVE_PROMPTS_SHOT_TYPE) >= 8
print('Task 1 PASS: 10 categories with all new keys, stability suffixes, negative prompts')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - CATEGORY_CONFIGS has exactly 10 keys: food_cookies, food_chocolate, food_burger, beauty_skincare, fashion_shoes, tech_electronics, beverage, jewelry_watches, candles_scented, supplements_bottles
    - Every category dict contains keys: display_name, surface, lighting, mood, lens, hero_actions, video_camera_moves, negative, color_grade, elements, atmospheres, micro_details
    - elements has 4-6 entries per category, atmospheres has 3-4, micro_details has 3-4
    - STABILITY_SUFFIXES dict exists with at least 8 shot-type keys
    - NEGATIVE_PROMPTS_SHOT_TYPE dict exists with at least 8 shot-type keys
    - NEGATIVE_PROMPTS_BASE string constant exists
    - CATEGORY_DEFAULT contains color_grade, elements, atmospheres, micro_details
    - All existing non-prompt constants (ADS_ENABLED through STYLE_VIDEO_MODEL, lines 1-69) are unchanged
    - VIDEO_QUALITY_SUFFIX still exists (backward compat) with deprecation comment
  </acceptance_criteria>
  <done>10 categories with full enriched configs, stability suffix dict, negative prompt dicts, all importable from config.py</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite SHOT_PLANS with Skool-level 400-500 char prompts and template variables</name>
  <files>src/product_studio/config.py</files>
  <read_first>
    - src/product_studio/config.py (after Task 1 — read current SHOT_PLANS structure)
    - ~/.claude/projects/-Users-luigivivian-meme-lab/memory/reference_skool_video_prompts.md (camera-as-sentence, physics micro-details, stability instructions)
    - ~/.claude/projects/-Users-luigivivian-meme-lab/memory/reference_skool_prompt_techniques.md (exhaustive scene description, color grade identity, atmosphere as depth)
  </read_first>
  <action>
Rewrite ALL SHOT_PLANS entries and add plans for the 3 new categories. Also add FEW_SHOT_EXAMPLES dict.

Each prompt MUST follow the Skool pattern (per D-01):
1. Opening with scene setup + surface + product placement via {product}
2. Camera movement as a FULL SENTENCE: "Camera performs a slow smooth dolly backward, gradually revealing..."
3. Lighting description as ACTION: "warm side light rakes across surfaces catching every texture"
4. Physics micro-detail: {micro_detail} template variable
5. Environmental atmosphere: {atmosphere} template variable
6. Lens + DOF specification: "Shot on 85mm lens, f/2.8, shallow depth of field with creamy bokeh"
7. Color grade identity from category
8. No stability suffix in the prompt text itself — that's appended at runtime from STABILITY_SUFFIXES

Template variables used in prompts (per D-24):
- {product} — product name/element reference (existing, keep)
- {hero_action} — drawn from category hero_actions list (per D-07)
- {atmosphere} — drawn from category atmospheres list
- {micro_detail} — drawn from category micro_details list

Target length: 400-500 chars per prompt.

For each of the 10 categories, write the shot plans:

FOOD categories (food_cookies, food_chocolate, food_burger) get 4-5 shots each (per D-06):
- Act 1: Macro texture reveal (static_macro)
- Act 2: Hero action moment (varies — static_macro or push_in)
- Act 3: Pull back context reveal (dolly_out)
- Act 4: Hero shot with space for text (push_in or static)
- Act 5 (optional, food only): Overhead hero shot or detail B-roll

NON-FOOD categories (beauty_skincare, fashion_shoes, tech_electronics, beverage, jewelry_watches, candles_scented, supplements_bottles) get 3 shots:
- Act 1: Detail/texture reveal (static_macro)
- Act 2: Context/orbit reveal (dolly_out or orbit)
- Act 3: Hero shot (push_in or orbit)

Example of a properly rewritten food_cookies prompt at 400-500 chars:
```
"Extreme macro of {product} on dark weathered slate surface, {micro_detail}. Camera holds perfectly locked on subject, macro lens f/2.8 capturing every crumb and texture ridge in sharp detail. {hero_action}. Warm side light rakes across at low angle, {atmosphere}. Shot on 100mm macro lens, paper-thin depth of field with smooth circular bokeh. {color_grade}."
```
(where {color_grade} is NOT a template var — hardcode the category's color_grade string directly into each prompt)

IMPORTANT: The color_grade string from the category config MUST appear literally in every prompt for that category (per D-27). Do NOT use a {color_grade} placeholder — write the actual string.

Also add SHOT_PLAN_DEFAULT with 3 generic shots using the same Skool-level structure.

Add FEW_SHOT_EXAMPLES dict (per D-15) after SHOT_PLANS — 3-5 example prompts per category archetype:
```python
FEW_SHOT_EXAMPLES: dict[str, list[str]] = {
    "food": [
        "Extreme macro of artisan sourdough bread on dark slate...",  # 400-500 chars each
        "Camera performs slow dolly backward from golden pastry...",
        "Overhead shot looking down at gourmet pizza...",
    ],
    "beauty": [
        "Close-up of luxury perfume bottle on wet marble...",
        "Slow orbit around skincare serum on polished surface...",
        "Camera pushes in toward moisturizer jar...",
    ],
    "tech": [
        "Close-up of premium headphones on reflective dark surface...",
        "Slow orbit revealing laptop thin profile...",
        "Camera pushes in toward smartwatch on dark surface...",
    ],
    "lifestyle": [
        "Product centered on natural linen surface...",
        "Slow dolly out from candle arrangement...",
        "Camera holds steady on supplement bottle...",
    ],
}
```
Each example must be a full 400-500 char Skool-level prompt demonstrating the pattern for its archetype.
  </action>
  <verify>
    <automated>cd /Users/luigivivian/meme-lab && python -c "
from src.product_studio.config import SHOT_PLANS, SHOT_PLAN_DEFAULT, FEW_SHOT_EXAMPLES
# Check all 10 categories have shot plans
assert len(SHOT_PLANS) == 10, f'Expected 10 shot plan categories, got {len(SHOT_PLANS)}'
# Check food categories have 4-5 shots
for cat in ['food_cookies', 'food_chocolate', 'food_burger']:
    n = len(SHOT_PLANS[cat])
    assert 4 <= n <= 5, f'{cat} has {n} shots (expected 4-5)'
# Check non-food have 3 shots
for cat in ['beauty_skincare', 'fashion_shoes', 'tech_electronics', 'beverage', 'jewelry_watches', 'candles_scented', 'supplements_bottles']:
    n = len(SHOT_PLANS[cat])
    assert n == 3, f'{cat} has {n} shots (expected 3)'
# Check prompt length 400-500 chars
for cat, shots in SHOT_PLANS.items():
    for i, shot in enumerate(shots):
        plen = len(shot['prompt'])
        assert 350 <= plen <= 550, f'{cat}[{i}] prompt is {plen} chars (expected ~400-500)'
# Check template variables present
for cat, shots in SHOT_PLANS.items():
    for i, shot in enumerate(shots):
        p = shot['prompt']
        assert '{product}' in p, f'{cat}[{i}] missing {{product}}'
# Check few-shot examples exist
assert len(FEW_SHOT_EXAMPLES) >= 3, f'Only {len(FEW_SHOT_EXAMPLES)} few-shot categories'
for archetype, examples in FEW_SHOT_EXAMPLES.items():
    assert len(examples) >= 3, f'{archetype} has only {len(examples)} examples'
    for j, ex in enumerate(examples):
        assert len(ex) >= 300, f'{archetype}[{j}] example only {len(ex)} chars'
# Check SHOT_PLAN_DEFAULT
assert len(SHOT_PLAN_DEFAULT) == 3
print('Task 2 PASS: 10 categories, food=4-5 shots, non-food=3, prompts 400-500 chars, vars present, few-shot examples')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - SHOT_PLANS has exactly 10 category keys
    - food_cookies, food_chocolate, food_burger each have 4-5 shot entries
    - beauty_skincare, fashion_shoes, tech_electronics, beverage, jewelry_watches, candles_scented, supplements_bottles each have 3 shot entries
    - Every shot prompt is 400-500 chars (350-550 tolerance for edge cases)
    - Every shot prompt contains {product} placeholder
    - Every shot prompt contains a camera movement described as a full sentence (not a label)
    - Every shot prompt contains a lens specification (focal length + aperture)
    - Every shot prompt contains the category's color_grade string literally
    - FEW_SHOT_EXAMPLES dict exists with at least 3 archetype keys, each with 3+ examples of 300+ chars
    - SHOT_PLAN_DEFAULT has 3 entries with Skool-level prompts
  </acceptance_criteria>
  <done>All 10 categories have professional shot plans at 400-500 chars each, food has 4-5 shots, non-food has 3, template variables present, few-shot examples ready for Gemini fallback</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.py constants -> prompt_builder | Trusted internal boundary — config values consumed by prompt builder |
| prompt text -> Kling API | Prompt text sent to external API — must not contain injection vectors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1003-01 | I (Information Disclosure) | FEW_SHOT_EXAMPLES | accept | Examples are generic product descriptions, no sensitive data. Low risk. |
| T-1003-02 | T (Tampering) | config.py constants | accept | Internal Python constants, not user-modifiable at runtime. Server-side only. |
</threat_model>

<verification>
- All 10 CATEGORY_CONFIGS importable with new keys (color_grade, elements, atmospheres, micro_details)
- All 10 SHOT_PLANS categories with correct shot counts (food 4-5, non-food 3)
- All prompts 400-500 chars with Skool patterns
- STABILITY_SUFFIXES, NEGATIVE_PROMPTS_SHOT_TYPE, NEGATIVE_PROMPTS_BASE importable
- FEW_SHOT_EXAMPLES importable with 3+ archetypes
- Existing constants unchanged (ADS_ENABLED through STYLE_VIDEO_MODEL)
</verification>

<success_criteria>
config.py is the complete single source of truth for all prompt content, with 10 enriched categories, professional shot plans, stability suffixes, negative prompt system, element libraries, and few-shot examples. All importable.
</success_criteria>

<output>
After completion, create `.planning/phases/1003-pro-video-prompt-engineering/1003-01-SUMMARY.md`
</output>
