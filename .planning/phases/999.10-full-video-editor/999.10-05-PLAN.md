---
phase: 999.10-full-video-editor
plan: 05
type: execute
wave: 3
depends_on: ["999.10-01", "999.10-04"]
files_modified:
  - memelab/src/components/editor/Timeline.tsx
  - memelab/src/components/editor/TimelineTrack.tsx
  - memelab/src/components/editor/TimelineBlock.tsx
  - memelab/src/components/editor/TimelineRuler.tsx
  - memelab/src/hooks/use-timeline-zoom.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Timeline shows 3 tracks: video, audio, subtitles (per D-04)"
    - "Video track shows scene blocks proportional to duration"
    - "Scenes can be reordered via drag-and-drop (per D-05, D-08)"
    - "Scene blocks have trim handles on edges (per D-09)"
    - "Timeline scrolls horizontally and zooms via pinch/scroll (per D-11)"
    - "Ruler shows frame/time markers synced with playhead"
    - "Clicking a scene selects it in the store"
  artifacts:
    - path: "memelab/src/components/editor/Timeline.tsx"
      provides: "Multi-track timeline container with DndContext"
      exports: ["Timeline"]
    - path: "memelab/src/components/editor/TimelineTrack.tsx"
      provides: "Single track row (video, audio, or subtitle)"
      exports: ["TimelineTrack"]
    - path: "memelab/src/components/editor/TimelineBlock.tsx"
      provides: "Draggable/sortable scene block with trim handles"
      exports: ["TimelineBlock"]
    - path: "memelab/src/components/editor/TimelineRuler.tsx"
      provides: "Time ruler with playhead indicator"
      exports: ["TimelineRuler"]
    - path: "memelab/src/hooks/use-timeline-zoom.ts"
      provides: "Zoom level and scroll state management"
      exports: ["useTimelineZoom"]
  key_links:
    - from: "memelab/src/components/editor/Timeline.tsx"
      to: "@dnd-kit/core"
      via: "DndContext for drag-and-drop"
      pattern: "DndContext"
    - from: "memelab/src/components/editor/TimelineBlock.tsx"
      to: "@dnd-kit/sortable"
      via: "useSortable hook"
      pattern: "useSortable"
    - from: "memelab/src/components/editor/Timeline.tsx"
      to: "memelab/src/stores/editor-store.ts"
      via: "reorderScenes on drag end"
      pattern: "reorderScenes"
---

<objective>
Build the multi-track timeline component with drag-and-drop scene reordering, trim handles, zoom, and playhead ruler. This is the primary interaction surface for the video editor.

Purpose: The timeline (per D-04 to D-11) is where users manipulate scenes, adjust timing, and see the structure of their reel. It connects to the Zustand store for all mutations.
Output: Fully interactive timeline with video/audio/subtitle tracks, drag reorder, trim handles, zoom/scroll
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.10-full-video-editor/999.10-CONTEXT.md
@.planning/phases/999.10-full-video-editor/999.10-RESEARCH.md
@.planning/phases/999.10-full-video-editor/999.10-01-SUMMARY.md

<interfaces>
From Plan 01 (editor-store.ts):
```typescript
// State
scenes: EditorScene[];
subtitles: EditorSubtitle[];
audioItems: EditorAudioItem[];
selectedSceneId: string | null;
selectedSubtitleId: string | null;
playheadFrame: number;

// Actions
reorderScenes(fromIndex: number, toIndex: number): void;
trimScene(sceneId: string, newDuration: number): void;
setSelectedScene(id: string | null): void;
setSelectedSubtitle(id: string | null): void;
setPlayheadFrame(frame: number): void;
```

From Plan 01 (editor-types.ts):
```typescript
export interface EditorScene {
  id: string; index: number; durationInFrames: number;
  clipUrl?: string; imgUrl?: string; narration: string;
  transition: { type: string; durationFrames: number };
  status: "ready" | "regenerating";
}
export interface EditorSubtitle { id: string; text: string; startFrame: number; endFrame: number; ... }
export interface EditorAudioItem { id: string; audioUrl: string; from: number; durationInFrames: number; }
export const EDITOR_FPS = 30;
```

From Plan 04 (EditorLayout.tsx):
```typescript
// Timeline slot is passed as children or named prop to EditorLayout
<EditorLayout timeline={<Timeline playerRef={playerRef} />} ... />
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create zoom hook, ruler, and timeline container</name>
  <files>memelab/src/hooks/use-timeline-zoom.ts, memelab/src/components/editor/TimelineRuler.tsx, memelab/src/components/editor/Timeline.tsx</files>
  <read_first>memelab/src/stores/editor-store.ts, memelab/src/stores/editor-types.ts, memelab/src/hooks/use-editor.ts</read_first>
  <action>
1. Create `memelab/src/hooks/use-timeline-zoom.ts`:
- State: `pixelsPerFrame` (default: 3, range 0.5-20), `scrollLeft` (default: 0)
- `zoomIn()`: multiply pixelsPerFrame by 1.5, clamp to max 20
- `zoomOut()`: divide pixelsPerFrame by 1.5, clamp to min 0.5
- `handleWheel(e: WheelEvent)`: if ctrlKey or metaKey (pinch), adjust zoom; otherwise adjust scrollLeft
- Return: { pixelsPerFrame, scrollLeft, setScrollLeft, zoomIn, zoomOut, handleWheel }

2. Create `memelab/src/components/editor/TimelineRuler.tsx`:
- "use client" directive
- Props: `{ pixelsPerFrame: number; totalFrames: number; playheadFrame: number; onSeek: (frame: number) => void; scrollLeft: number }`
- Render horizontal ruler bar showing time markers (every 1s = 30 frames at EDITOR_FPS)
- Format labels as "0:00", "0:01", "0:02", etc.
- Red vertical line at playheadFrame position: `left: playheadFrame * pixelsPerFrame - scrollLeft`
- Click on ruler to seek: calculate frame from click position
- Fixed height: h-6, bg-zinc-900, border-b

3. Create `memelab/src/components/editor/Timeline.tsx`:
- "use client" directive
- Props: `{ playerRef: React.RefObject<PlayerRef> }`
- Use `useTimelineZoom()` hook for zoom state
- Use `useEditorStore` for scenes, subtitles, audioItems, selectedSceneId, playheadFrame
- Use `useTotalDuration()` for total frames
- Wrap video track in `<DndContext>` from @dnd-kit/core with `closestCenter` collision detection
- `onDragEnd` handler: extract old/new index from event, call `reorderScenes(oldIndex, newIndex)`
- Wrap scenes in `<SortableContext items={sceneIds} strategy={horizontalListSortingStrategy}>`
- Layout:
  - Container: `flex flex-col border-t bg-zinc-950`
  - TimelineRuler at top
  - Scrollable area with 3 rows: video track, audio track, subtitle track
  - Each track is a `<TimelineTrack>` with label ("Video", "Audio", "Legendas")
  - Track labels column fixed left (w-20), track content scrolls horizontally
- Horizontal scroll (per D-11): attach wheel handler to scrollable container with `{ passive: false }` on the wheel listener (same pattern as pipeline-diagram.tsx)
- Sync playhead position with PlayerRef: listen to Player "timeupdate" event via playerRef, update `setPlayheadFrame(frame)`
  </action>
  <verify>
    <automated>cd /Users/luigivivian/meme-lab/memelab && grep "DndContext" src/components/editor/Timeline.tsx && grep "SortableContext" src/components/editor/Timeline.tsx && grep "pixelsPerFrame" src/hooks/use-timeline-zoom.ts && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - Timeline.tsx wraps video track in DndContext + SortableContext
    - onDragEnd calls reorderScenes from store
    - TimelineRuler shows time markers and playhead position
    - Clicking ruler seeks to frame position
    - use-timeline-zoom.ts manages pixelsPerFrame with zoom in/out and wheel handler
    - Horizontal scroll uses passive:false on wheel listener
    - Three track rows visible (video, audio, subtitle)
  </acceptance_criteria>
  <done>Timeline container renders 3 tracks with zoom, ruler, playhead, and DndContext for drag-and-drop.</done>
</task>

<task type="auto">
  <name>Task 2: Create TimelineTrack and TimelineBlock with trim handles</name>
  <files>memelab/src/components/editor/TimelineTrack.tsx, memelab/src/components/editor/TimelineBlock.tsx</files>
  <read_first>memelab/src/components/editor/Timeline.tsx, memelab/src/stores/editor-store.ts, memelab/src/stores/editor-types.ts</read_first>
  <action>
1. Create `memelab/src/components/editor/TimelineTrack.tsx`:
- "use client" directive
- Props: `{ type: "video" | "audio" | "subtitle"; items: EditorScene[] | EditorAudioItem[] | EditorSubtitle[]; pixelsPerFrame: number; selectedId: string | null; onSelect: (id: string) => void }`
- Render label column (w-20, fixed): track name + icon (Film for video, Volume2 for audio, Subtitles for subtitle) from lucide-react
- Content area: horizontal flex row of TimelineBlock components
- For video track: blocks are sortable (wrapped in SortableContext from parent)
- For audio/subtitle tracks: blocks are positioned by `from` or `startFrame` * pixelsPerFrame
- Track styling: h-14 for video/audio, h-10 for subtitles, alternating bg-zinc-900/bg-zinc-950

2. Create `memelab/src/components/editor/TimelineBlock.tsx`:
- "use client" directive
- Props: `{ item: EditorScene | EditorAudioItem | EditorSubtitle; pixelsPerFrame: number; selected: boolean; onSelect: () => void; onTrim?: (newDurationFrames: number) => void; trackType: "video" | "audio" | "subtitle" }`
- For video scenes (per D-05, D-08, D-09):
  - Use `useSortable({ id: item.id })` from @dnd-kit/sortable
  - Apply CSS.Transform.toString(transform) for drag animation
  - Width = item.durationInFrames * pixelsPerFrame
  - Center area: `{...listeners}` for drag to reorder (per D-08)
  - Left trim handle: 4px wide, `cursor-col-resize`, onPointerDown starts trim tracking (per D-09). On pointer move, calculate delta in frames, call onTrim with new duration. Min duration 15 frames (0.5s). Trim from left adjusts both start time and duration.
  - Right trim handle: same but simpler — just adjusts duration from the right
  - Show scene thumbnail: small preview image or scene index number
  - Selected state: ring-2 ring-purple-500
  - Regenerating state (per D-18): spinner overlay on block, reduced opacity
- For subtitle blocks:
  - Positioned absolutely by startFrame * pixelsPerFrame
  - Width = (endFrame - startFrame) * pixelsPerFrame
  - Show truncated text
  - Click to select (calls onSelect)
  - No drag (subtitle timing adjusted in properties panel or by dragging block edges)
- For audio blocks:
  - Positioned by from * pixelsPerFrame
  - Width = durationInFrames * pixelsPerFrame
  - Gradient fill representing audio presence
  - Click to select
- Color scheme: video=purple-500/20, audio=blue-500/20, subtitle=amber-500/20
  </action>
  <verify>
    <automated>cd /Users/luigivivian/meme-lab/memelab && grep "useSortable" src/components/editor/TimelineBlock.tsx && grep "cursor-col-resize" src/components/editor/TimelineBlock.tsx && grep "TimelineBlock" src/components/editor/TimelineTrack.tsx && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - TimelineBlock uses useSortable for video blocks (drag reorder per D-08)
    - Trim handles render on left and right edges with cursor-col-resize (per D-09)
    - Trim handles track pointer movement and call onTrim with new duration
    - Block width = durationInFrames * pixelsPerFrame
    - Selected state shows ring highlight
    - Regenerating scenes show spinner (per D-18)
    - TimelineTrack renders row of TimelineBlocks with track label
    - Different track types have appropriate styling
  </acceptance_criteria>
  <done>Timeline blocks support drag reorder and edge-trim. Three track types render correctly with selection, trim, and regenerating states.</done>
</task>

</tasks>

<verification>
- `cd memelab && npx tsc --noEmit` — no type errors
- Timeline renders 3 tracks with blocks
- Drag reorder triggers store.reorderScenes
- Trim handles adjust scene duration
- Zoom changes block sizes
- Playhead syncs with Player
</verification>

<success_criteria>
- Multi-track timeline with video, audio, subtitle tracks (per D-04)
- Scene blocks proportional to duration, reorderable via drag (per D-05, D-08)
- Trim handles on block edges (per D-09)
- Horizontal scroll and pinch/scroll zoom (per D-11)
- Ruler with time markers and playhead
- Clicking scene selects it in store
- Regenerating scenes show spinner overlay (per D-18)
</success_criteria>

<output>
After completion, create `.planning/phases/999.10-full-video-editor/999.10-05-SUMMARY.md`
</output>
