---
phase: 999.10-full-video-editor
plan: 08
type: execute
wave: 4
depends_on: ["999.10-07"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Editor loads and displays existing reel scenes"
    - "Preview plays video with transitions and subtitles"
    - "Scenes can be reordered, trimmed, duplicated, deleted"
    - "Subtitle text editable inline and via panel"
    - "Undo/redo works across all operations"
    - "Autosave persists state to backend"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete video editor. Test all major user flows end-to-end.

Purpose: Verify that the editor works as a cohesive whole -- preview plays correctly, timeline interactions feel responsive, drag-and-drop works, autosave persists, and the overall UX matches the VEED.io-style vision.
Output: Confirmed working editor or list of issues to fix
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/999.10-full-video-editor/999.10-CONTEXT.md
@.planning/phases/999.10-full-video-editor/999.10-07-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify complete video editor</name>
  <files>memelab/src/app/(app)/reels/[jobId]/edit/page.tsx</files>
  <action>
Human verifies the complete video editor by testing all major user flows:

1. Start both servers: `cd memelab && npm run dev` (port 3000) and Python backend on port 8000
2. Navigate to `/reels` and find a completed reel with video
3. Click "Editar Video" button on the reel card or in the step-video view
4. Verify editor loads at `/reels/[id]/edit`:
   - Desktop-only gate: resize browser below 1024px, should show "use desktop" message
   - Layout: preview (9:16 vertical) at top, timeline below, properties panel on right

5. Test preview: Play/pause button works, video plays with scene transitions, subtitles appear at correct times

6. Test timeline: Three tracks visible (Video, Audio, Legendas), drag scene to reorder, drag edge to trim, zoom with Ctrl+scroll, scroll horizontally, click ruler to seek

7. Test scene operations: Click scene to select, right-click for context menu (Duplicar, Deletar, Dividir, Transicao), duplicate creates new scene, delete removes scene

8. Test properties panel: Selected scene shows duration slider, transition controls, voice config. Subtitle selection shows font, color, size, shadow, position controls.

9. Test subtitle editing: Click subtitle in preview overlay, edit text inline, drag to reposition

10. Test undo/redo: Make changes, click undo to revert, click redo to re-apply

11. Test autosave: Make a change, wait 2s, see "Salvando..." then "Salvo", reload page to verify state restored

12. Test export: Click Export in toolbar, verify Remotion render triggers
  </action>
  <verify>
    <automated>cd /Users/luigivivian/meme-lab/memelab && npx tsc --noEmit 2>&1 | tail -5 && npx vitest run --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <done>Human confirms editor works: preview plays, timeline interactive, scene ops functional, undo/redo works, autosave persists. Type "approved" or describe issues.</done>
</task>

</tasks>

<verification>
Human verification of all 12 test areas above.
</verification>

<success_criteria>
- Editor loads existing reel data correctly
- Preview plays with transitions and subtitles
- Timeline is interactive: drag reorder, trim, zoom, scroll
- All scene operations work: duplicate, delete, split, transition change
- Properties panel edits scene and subtitle properties
- Undo/redo works across operations
- Autosave persists and restores state
- Entry points accessible from reels workflow
</success_criteria>

<output>
After completion, create `.planning/phases/999.10-full-video-editor/999.10-08-SUMMARY.md`
</output>
