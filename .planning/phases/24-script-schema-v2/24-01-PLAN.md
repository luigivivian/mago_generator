---
phase: 24-script-schema-v2
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_reels_script_schema.py
autonomous: true
requirements:
  - SCRIPT-01
  - SCRIPT-02
  - SCRIPT-03
  - SCRIPT-04
  - SCRIPT-05
  - SCRIPT-06
must_haves:
  truths:
    - "All 10 test stubs exist and the suite is GREEN (xfail stubs pass)"
    - "Each SCRIPT-01..SCRIPT-06 requirement has at least one mapped test"
  artifacts:
    - path: "tests/test_reels_script_schema.py"
      provides: "Phase 24 validation suite with 10 xfail test stubs"
      min_lines: 80
  key_links:
    - from: "tests/test_reels_script_schema.py"
      to: "src/reels_pipeline/script_gen.py"
      via: "imports ROTEIRO_SCHEMA, generate_script"
      pattern: "from src.reels_pipeline"
---

<objective>
Create the Phase 24 validation suite as xfail test stubs, following the established Phase 22/23 pattern. All 10 tests start as xfail so the suite stays GREEN. Later waves flip tests to active as features land.

Purpose: Binding contract between plan task verifications and the test runner. Every requirement has a mapped test before any implementation begins.
Output: `tests/test_reels_script_schema.py` with 10 xfail stubs (6 requirement tests + 4 extras)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-script-schema-v2/24-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create xfail test stubs for SCRIPT-01 through SCRIPT-06 plus extras</name>
  <files>tests/test_reels_script_schema.py</files>
  <read_first>
    - tests/test_reels_tts.py (reference: xfail stub pattern from Phase 22)
    - tests/test_reels_timing.py (reference: xfail stub pattern from Phase 23)
    - .planning/phases/24-script-schema-v2/24-RESEARCH.md (test map in Validation Architecture section)
  </read_first>
  <action>
Create `tests/test_reels_script_schema.py` with 10 xfail test stubs. Follow the exact pattern from Phase 22 (`tests/test_reels_tts.py`) where each test is decorated with `@pytest.mark.xfail(reason="Wave N stub", strict=True)`.

Module docstring:
```
"""Phase 24: Script Schema v2 -- validation suite.

Wave 0 (plan 24-01) creates all 10 tests as xfail stubs.
Waves 1-3 (plans 24-02..24-04) flip owned tests from xfail -> active.

DO NOT rename a test without updating 24-VALIDATION.md.
"""
```

Test stubs (all xfail with `reason="Wave N stub"` and `strict=True`):

1. `test_01_character_card_in_schema` (SCRIPT-01, Wave 1 stub)
   - Will verify: ROTEIRO_SCHEMA dict has `character_card` property with `description` and `style_seed` sub-properties, and `character_card` is NOT in the top-level `required` list
   - Body: `pytest.fail("Wave 1 will implement")`

2. `test_02_image_prompt_per_cena` (SCRIPT-02, Wave 1 stub)
   - Will verify: ROTEIRO_SCHEMA cenas.items.properties contains `image_prompt` of type STRING, and `image_prompt` is in cenas.items.required
   - Body: `pytest.fail("Wave 1 will implement")`

3. `test_03_mood_enum` (SCRIPT-03, Wave 1 stub)
   - Will verify: ROTEIRO_SCHEMA cenas.items.properties contains `mood` with type STRING and enum list of exactly 7 values: `["mysterious", "dramatic", "hopeful", "tense", "calm", "sad", "epic"]`
   - Body: `pytest.fail("Wave 1 will implement")`

4. `test_04_transitions_enum` (SCRIPT-04, Wave 1 stub)
   - Will verify: ROTEIRO_SCHEMA cenas.items.properties contains `transition_in` and `transition_out`, each with type STRING and enum `["fade", "cut", "dissolve", "slide"]`, both in cenas.items.required
   - Body: `pytest.fail("Wave 1 will implement")`

5. `test_05_system_prompts_updated` (SCRIPT-05, Wave 2 stub)
   - Will verify: all 7 prompt templates (_SYSTEM_PROMPTS 3 langs + _BIBLE_SYSTEM_PROMPTS 3 langs + _SYSTEM_PROMPT_FALLBACK) contain the string "image_prompt" and "mood"
   - Body: `pytest.fail("Wave 2 will implement")`

6. `test_06_legacy_migration` (SCRIPT-06, Wave 3 stub)
   - Will verify: `migrate_legacy_roteiro()` adds `mood="calm"`, `transition_in="fade"`, `transition_out="fade"`, and `image_prompt` derived from `legenda_overlay` to every cena missing those fields
   - Body: `pytest.fail("Wave 3 will implement")`

7. `test_07_migration_idempotent` (extra, Wave 3 stub)
   - Will verify: running `migrate_legacy_roteiro()` twice produces same result (second run is no-op)
   - Body: `pytest.fail("Wave 3 will implement")`

8. `test_08_manual_bible_script_has_v2_fields` (extra, Wave 3 stub)
   - Will verify: `parse_manual_script()` output cenas contain `image_prompt`, `mood`, `transition_in`, `transition_out`
   - Body: `pytest.fail("Wave 3 will implement")`

9. `test_09_schema_character_card_optional` (extra, Wave 1 stub)
   - Will verify: `character_card` is NOT in ROTEIRO_SCHEMA["required"] list (optional field)
   - Body: `pytest.fail("Wave 1 will implement")`

10. `test_10_image_prompt_distinct_from_overlay` (extra, Wave 2 stub)
    - Will verify: system prompts contain instruction text distinguishing `image_prompt` from `legenda_overlay` (grep for "image_prompt" AND "legenda_overlay" in each prompt)
    - Body: `pytest.fail("Wave 2 will implement")`

Each test gets a docstring with the requirement ID it maps to (e.g., `"""SCRIPT-01: character_card present in schema."""`).

Imports needed:
```python
from __future__ import annotations
import pytest
```

No other imports at this stage -- tests are pure stubs.
  </action>
  <verify>
    <automated>cd /Users/luigivivian/meme-lab && python3 -m pytest tests/test_reels_script_schema.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_reels_script_schema.py exists and contains exactly 10 test functions
    - `grep -c "def test_" tests/test_reels_script_schema.py` returns 10
    - `grep -c "xfail" tests/test_reels_script_schema.py` returns 10
    - `python3 -m pytest tests/test_reels_script_schema.py -x -q` exits 0 with "10 xfailed" in output
    - `grep "SCRIPT-01" tests/test_reels_script_schema.py` finds at least 1 match
    - `grep "SCRIPT-06" tests/test_reels_script_schema.py` finds at least 1 match
  </acceptance_criteria>
  <done>10 xfail test stubs exist, suite is GREEN (all xfail), every SCRIPT-01..SCRIPT-06 requirement has at least one test</done>
</task>

</tasks>

<verification>
- `python3 -m pytest tests/test_reels_script_schema.py -v` shows 10 xfailed, 0 failed, 0 passed
- No other test files broken: `python3 -m pytest tests/ -x -q --ignore=tests/test_agents_quick.py`
</verification>

<success_criteria>
- 10 xfail test stubs in tests/test_reels_script_schema.py
- Suite GREEN (all xfail, no failures)
- Each SCRIPT-01..SCRIPT-06 requirement mapped to at least one test
</success_criteria>

<output>
After completion, create `.planning/phases/24-script-schema-v2/24-01-SUMMARY.md`
</output>
