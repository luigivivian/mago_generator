---
phase: 999.9-kie-api-credit-system
plan: 01
subsystem: database, api
tags: [credits, sqlalchemy, alembic, kie-api, async]

requires:
  - phase: 999.1
    provides: "VIDEO_MODELS dict with per-model BRL pricing, KieSora2Client"
  - phase: 20
    provides: "compute_video_cost_brl pattern, api_usage tracking"
provides:
  - "UserCredit and CreditLog ORM models"
  - "Migration 027 creating user_credits and credit_logs tables"
  - "CREDIT_COSTS dict mapping 11 models to credit costs"
  - "compute_credit_cost function with closest-duration snap"
  - "CreditService with check_and_deduct, refund, top_up, get_balance"
  - "InsufficientCreditsError exception for gating"
affects: [999.9-02, 999.9-03, 999.9-04]

tech-stack:
  added: []
  patterns: ["CreditService atomic balance operations with FOR UPDATE locking", "credit_logs audit trail for all Kie API calls"]

key-files:
  created:
    - "src/services/credit_service.py"
    - "src/database/migrations/versions/027_credit_system.py"
    - "tests/test_credit_service.py"
  modified:
    - "src/database/models.py"
    - "config.py"

key-decisions:
  - "CreditService at service layer (not middleware) because background tasks bypass HTTP middleware"
  - "Integer credits to avoid floating point rounding ($7=1000 credits, $0.007/credit)"
  - "SELECT FOR UPDATE for row locking on MySQL, SQLite falls back to single-writer serialization"
  - "Deduct before API call, refund on failure (prevents overspend)"

patterns-established:
  - "CreditService(session) pattern: inject async session, all operations atomic with flush"
  - "credit_logs audit trail: every operation (deduction, blocked, refund, top_up) gets a log entry"
  - "CREDIT_COSTS dict: per-model credit mapping derived from BRL prices / 5.75 / 0.007"

requirements-completed: [CRED-01, CRED-02, CRED-03, CRED-04, CRED-08]

duration: 4min
completed: 2026-04-02
---

# Phase 999.9 Plan 01: Credit System Foundation Summary

**Pre-paid credit service with per-model cost mapping, UserCredit/CreditLog models, atomic balance operations, and 16 passing tests**

## Performance

- **Duration:** 4 min
- **Started:** 2026-04-02T22:14:20Z
- **Completed:** 2026-04-02T22:18:42Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments
- UserCredit and CreditLog ORM models with migration 027
- CREDIT_COSTS dict mapping all 11 models (10 video + suno) to credit costs
- CreditService with check_and_deduct, refund, top_up, get_balance
- 16 unit tests passing (9 model/config + 7 async service tests)

## Task Commits

Each task was committed atomically:

1. **Task 1: Database models, migration, and CREDIT_COSTS config** - `b9772db` (feat)
2. **Task 2: CreditService class with unit tests** - `aa1c0a1` (feat)

## Files Created/Modified
- `src/database/models.py` - Added UserCredit (section 14b) and CreditLog (section 14c) models, User.credits relationship
- `src/database/migrations/versions/027_credit_system.py` - Creates user_credits and credit_logs tables with indexes
- `config.py` - Added CREDIT_COSTS dict (11 models) and compute_credit_cost function
- `src/services/credit_service.py` - CreditService with atomic check_and_deduct, refund, top_up, get_balance
- `tests/test_credit_service.py` - 16 unit tests covering models, config, and async service operations

## Decisions Made
- CreditService at service layer (not middleware) because background tasks bypass HTTP middleware
- Integer credits avoid floating point rounding ($7=1000 credits, $0.007/credit)
- SELECT FOR UPDATE for MySQL row locking; SQLite falls back to single-writer serialization
- Deduct before API call, refund on failure pattern (prevents overspend)

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- CreditService and models ready for Plan 02 (API gating integration)
- All three Kie API call paths (video, reels, ads) can now import CreditService
- compute_credit_cost available for pre-call cost estimation

---
*Phase: 999.9-kie-api-credit-system*
*Completed: 2026-04-02*
