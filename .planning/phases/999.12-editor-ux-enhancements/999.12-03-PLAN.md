---
phase: 999.12-editor-ux-enhancements
plan: 03
type: execute
wave: 3
depends_on: ["999.12-01"]
files_modified:
  - memelab/src/stores/editor-types.ts
  - memelab/src/stores/editor-store.ts
  - memelab/src/components/editor/PropertiesPanel.tsx
  - memelab/src/components/editor/Timeline.tsx
  - memelab/src/components/editor/Toolbar.tsx
  - memelab/src/hooks/use-timeline-zoom.ts
  - memelab/src/remotion/ReelComposition.tsx
autonomous: true

must_haves:
  truths:
    - "Per-clip volume slider on selected audio item, respected by Remotion <Audio>"
    - "Per-track mute and solo toggles in the timeline track labels"
    - "Mute hides a track from the preview, solo mutes all other tracks"
    - "Zoom-to-fit button in toolbar resets timeline to fit total duration in viewport"
    - "Settings persist in localStorage across reloads (mute/solo/volume changes flow through autosave)"
  artifacts: []
  key_links: []
---

<objective>
Add per-clip volume control on audio items, per-track mute/solo, and a zoom-to-fit button (D-09, D-10, D-13, D-19, D-20).
</objective>

<context>
@.planning/phases/999.12-editor-ux-enhancements/999.12-CONTEXT.md
@memelab/src/stores/editor-store.ts
@memelab/src/components/editor/Toolbar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add volume + mute/solo state to types and store</name>
  <files>memelab/src/stores/editor-types.ts, memelab/src/stores/editor-store.ts</files>
  <action>
1. In editor-types.ts:
   - Add `volume?: number` (0..1) to `EditorAudioItem`. Default semantics:
     `undefined` means full volume (1).
   - Add `export type TrackKind = "video" | "audio" | "subtitle"` (already
     used in TimelineTrack — formalize as a type export).

2. In editor-store.ts:
   - New state fields:
     - `mutedTracks: Set<TrackKind>`
     - `soloedTracks: Set<TrackKind>`
   - Initialize both as empty Sets.
   - New actions:
     - `setAudioVolume(audioId: string, volume: number)` — clamps 0..1
     - `toggleTrackMute(track: TrackKind)`
     - `toggleTrackSolo(track: TrackKind)` — toggling solo on a track also
       clears mute on the same track
   - Exclude `mutedTracks`/`soloedTracks` from temporal partialize (UI
     state, not document state — same rationale as `selection`).
  </action>
  <verify>
    <automated>cd /Users/luigivivian/meme-lab/memelab && npx tsc --noEmit 2>&1 | grep -E "(editor-store|editor-types)" | head</automated>
  </verify>
  <done>Volume/mute/solo state added, store actions clamp and toggle correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Per-clip volume slider in PropertiesPanel for audio selection</name>
  <files>memelab/src/components/editor/PropertiesPanel.tsx</files>
  <action>
The current PropertiesPanel handles scenes and subtitles but not audio
items. Add a small `AudioPanel` that:
- Reads the selected audio item via `useEditorStore((s) => s.audioItems.find(a => a.id === selectedAudioId))`
- Renders a volume slider 0..100% (mapped to 0..1) using the existing
  `SliderField` component
- Calls `setAudioVolume(item.id, value/100)`

Wire it into the main `PropertiesPanel` render: when single-select and
the selected item is audio (i.e., `selectedAudioId` set), render
`<AudioPanel/>` instead of the empty fallback. The existing scene/sub
branches stay unchanged.
  </action>
  <verify>
    <automated>cd /Users/luigivivian/meme-lab/memelab && npx tsc --noEmit 2>&1 | grep PropertiesPanel | head -10</automated>
  </verify>
  <done>Selecting an audio item shows a volume slider that updates state and persists via existing autosave.</done>
</task>

<task type="auto">
  <name>Task 3: Mute/solo toggles in Timeline track labels</name>
  <files>memelab/src/components/editor/Timeline.tsx</files>
  <action>
In the existing track-label column section (Video/Audio/Legendas
buttons), append two tiny letter buttons after each label:
- `M` — mute toggle, calls `toggleTrackMute("video"|"audio"|"subtitle")`
- `S` — solo toggle, calls `toggleTrackSolo(...)`
- Visual: yellow when active, dim zinc when inactive. Use `text-[10px]
  font-bold w-4 h-4 rounded-sm` for compact look.
- Stop click propagation so they don't trigger the existing
  "select first item of this track" handler.
  </action>
  <verify>
    <automated>cd /Users/luigivivian/meme-lab/memelab && npx tsc --noEmit 2>&1 | grep Timeline\\.tsx | head -10</automated>
  </verify>
  <done>M/S buttons appear next to each track label and toggle the corresponding sets.</done>
</task>

<task type="auto">
  <name>Task 4: Apply mute/solo + per-clip volume in Remotion preview</name>
  <files>memelab/src/remotion/ReelComposition.tsx</files>
  <action>
1. Read mute/solo sets from the editor store (the composition is
   already a "use client" boundary because it's rendered in the Player).
2. Compute effective audibility per track:
   - if `soloedTracks.size > 0` and !`soloedTracks.has(track)` → silent
   - else if `mutedTracks.has(track)` → silent
3. For each `<Audio>` element rendered in the composition:
   - Pass `volume={item.volume ?? 1}` (D-09).
   - If audio track is silent per the rule above, pass `volume={0}` (or
     skip rendering — `volume={0}` is simpler and avoids re-mounts).
4. For the video track: if silent, render the visuals but skip any
   per-scene audio (currently all audio comes from the audio track).
5. For the subtitle track: subtitles render via the SubtitleEditor
   overlay, not inside the Remotion composition. Mute/solo for the
   subtitle track just hides them from the overlay — handle in
   `SubtitleEditor` next task or skip and treat subtitle mute as a
   no-op visual hide (acceptable since they're text).

For the simplest correct implementation: focus on audio track mute/solo
and per-clip volume. Subtitle/video mute can be follow-up work.
  </action>
  <verify>
    <automated>cd /Users/luigivivian/meme-lab/memelab && npx tsc --noEmit 2>&1 | grep ReelComposition | head -10</automated>
  </verify>
  <done>Audio Track elements receive volume from item.volume × track audibility flag.</done>
</task>

<task type="auto">
  <name>Task 5: Zoom-to-fit button in Toolbar + hook support</name>
  <files>memelab/src/hooks/use-timeline-zoom.ts, memelab/src/components/editor/Toolbar.tsx</files>
  <action>
1. In `use-timeline-zoom.ts`:
   - Export a new `fitToView(totalFrames: number, viewportWidth: number)`
     function that computes `pixelsPerFrame = clamp(viewportWidth / totalFrames, MIN, MAX)` and sets it.

2. In `Toolbar.tsx`:
   - Add a "Fit" button next to undo/redo/edit buttons (or right after
     edit buttons).
   - The Toolbar doesn't have a viewport width handle — read it from
     `document.getElementById('timeline-scroll')?.clientWidth` (add a
     stable id to the timeline scroll element in Timeline.tsx) OR
     simpler: dispatch a custom event `timeline:fit` and let Timeline.tsx
     handle it via a window listener inside its existing useEffect.

   Pick the custom event approach — keeps the Toolbar decoupled from
   timeline DOM. Add the listener in Timeline.tsx that calls
   `fitToView(totalFrames, scrollRef.current.clientWidth)`.
  </action>
  <verify>
    <automated>cd /Users/luigivivian/meme-lab/memelab && npx tsc --noEmit 2>&1 | grep -E "(Toolbar|use-timeline-zoom|Timeline\.tsx)" | head -10</automated>
  </verify>
  <done>"Fit" button in toolbar triggers a custom event, Timeline handles it by computing and setting the right ppf.</done>
</task>

</tasks>

<verification>
TypeScript clean for all 7 modified files. Manual smoke: select audio
item → volume slider works; toggle audio mute → no sound on play; toggle
solo → only that track plays; click Fit → entire timeline visible.
</verification>

<success_criteria>
- EditorAudioItem has optional `volume`
- mutedTracks / soloedTracks Sets in store
- AudioPanel in PropertiesPanel with volume slider
- M/S buttons in Timeline track labels
- ReelComposition respects volume + mute/solo on audio
- "Fit" button in Toolbar dispatches event handled by Timeline
- TypeScript clean, store tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/999.12-editor-ux-enhancements/999.12-03-SUMMARY.md`
</output>
