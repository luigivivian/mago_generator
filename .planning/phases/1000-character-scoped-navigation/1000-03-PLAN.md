---
phase: 1000-character-scoped-navigation
plan: 03
type: execute
wave: 3
depends_on: ["1000-02"]
files_modified:
  - memelab/src/components/layout/sidebar.tsx
  - memelab/src/contexts/character-context.tsx
autonomous: true
requirements:
  - CHAR-01
  - CHAR-02

must_haves:
  truths:
    - "CharacterSelector shows 'Todos os Personagens' as the first option"
    - "Selecting 'Todos' sets activeSlug to empty string"
    - "Selecting a character sets activeSlug to that character's slug"
    - "The selector visually indicates which option is currently active"
    - "CharacterContext defaults to empty string so new users see all content"
  artifacts:
    - path: "memelab/src/components/layout/sidebar.tsx"
      provides: "Todos os Personagens option in CharacterSelector"
      contains: "Todos"
    - path: "memelab/src/contexts/character-context.tsx"
      provides: "Empty string default for activeSlug"
      contains: 'useState("")'
  key_links:
    - from: "memelab/src/components/layout/sidebar.tsx"
      to: "memelab/src/contexts/character-context.tsx"
      via: "setActiveSlug('')"
      pattern: "setActiveSlug"
    - from: "memelab/src/contexts/character-context.tsx"
      to: "memelab/src/hooks/use-api.ts"
      via: "activeSlug flows to SWR hooks (wired in Plan 02)"
      pattern: "activeSlug"
---

<objective>
Add a "Todos os Personagens" option to the character selector dropdown in the sidebar AND change the CharacterContext default to empty string, both in a single atomic change. This ensures the "all characters" behavior lands together with the UI to select it.

Purpose: Without this, users are forced to always view content filtered by a single character with no way to see everything. The default changes from "mago-mestre" to "" so new/returning users see all content, with the "Todos" option visible to explain why.
Output: Updated CharacterSelector with "Todos" option. CharacterContext defaults to "".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/1000-character-scoped-navigation/1000-RESEARCH.md
@.planning/phases/1000-character-scoped-navigation/1000-02-SUMMARY.md

<interfaces>
From memelab/src/contexts/character-context.tsx (BEFORE this plan):
```typescript
// activeSlug defaults to "mago-mestre" — this plan changes it to ""
// setActiveSlug("") = select all
// setActiveSlug("mago-mestre") = select specific character
interface CharacterContextType {
  characters: CharacterSummary[];
  activeSlug: string;
  setActiveSlug: (slug: string) => void;
  activeCharacter: CharacterSummary | null;
  isLoading: boolean;
}
```

From Plan 02 (already completed): All SWR hooks include `character_slug` in cache keys. When `activeSlug` is empty string, `activeSlug || undefined` evaluates to `undefined`, so the API param is omitted and no character filter is applied.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add "Todos os Personagens" option and change CharacterContext default</name>
  <files>
    memelab/src/components/layout/sidebar.tsx,
    memelab/src/contexts/character-context.tsx
  </files>
  <read_first>
    memelab/src/components/layout/sidebar.tsx,
    memelab/src/contexts/character-context.tsx
  </read_first>
  <action>
**BOTH changes must land in this single task so the default change and the UI option are atomic.**

**A. Change CharacterContext default** (character-context.tsx):
- Change default activeSlug from `"mago-mestre"` to `""` (empty string = all characters).
- When `stored` is null in localStorage (no previous selection), keep the empty string default (= all).
- Update `activeCharacter` derivation: when activeSlug is empty, set activeCharacter to null.
- This means empty string = "Todos os Personagens" = no filter applied.

**B. Add "Todos os Personagens" to CharacterSelector** (sidebar.tsx):
Find the `CharacterSelector` component in sidebar.tsx. It renders a dropdown/list of characters from `useCharacterContext()`.

Add a "Todos os Personagens" option at the top of the character list:
- When the selector renders, prepend a special entry BEFORE the character list
- This entry should display "Todos os Personagens" (or "Todos" in compact/collapsed mode)
- Use an icon that suggests "all" — e.g., Lucide's `Users` icon or `LayoutGrid` icon
- Clicking it calls `setActiveSlug("")` (empty string)
- It should be visually highlighted when `activeSlug === ""`
- Separate it from character entries with a subtle divider (a thin border-t or margin)

The exact implementation depends on the current selector structure (dropdown, list, etc.). Read the component first and adapt accordingly.

If the selector uses a `<select>` element, add an `<option value="">Todos os Personagens</option>` as the first option.

If the selector uses custom buttons/cards, add a button before the character map that calls `setActiveSlug("")` with the "Todos" label.

**Visual styling:** Use the same active state styling as individual characters (e.g., highlighted background, border accent) when `activeSlug === ""`.
  </action>
  <verify>
    <automated>cd /Users/luigivivian/meme-lab/memelab && grep "Todos" src/components/layout/sidebar.tsx && grep '""' src/contexts/character-context.tsx | head -5 && npx next lint 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Todos" memelab/src/components/layout/sidebar.tsx` returns a match showing "Todos os Personagens" or "Todos"
    - `grep 'setActiveSlug.*""' memelab/src/components/layout/sidebar.tsx` returns a match (empty string for all)
    - CharacterContext default activeSlug is now "" (empty string), not "mago-mestre"
    - `npx next lint` passes
  </acceptance_criteria>
  <done>CharacterSelector has "Todos os Personagens" as first option. Clicking it sets activeSlug to empty string. CharacterContext defaults to "" so all pages show all content until a character is explicitly selected. Both changes are atomic.</done>
</task>

</tasks>

<verification>
- `npx next lint` passes
- "Todos os Personagens" appears in sidebar.tsx CharacterSelector
- setActiveSlug("") is called when "Todos" is selected
- CharacterContext default is "" (empty string)
- All pages (wired in Plan 02) correctly show all content when activeSlug is ""
</verification>

<success_criteria>
Users can select "Todos os Personagens" to see all content, or select a specific character to filter. The active state is visually clear. New users default to seeing all content. The default change and UI option land atomically so there is no state where the default is "" but no "Todos" option exists.
</success_criteria>

<output>
After completion, create `.planning/phases/1000-character-scoped-navigation/1000-03-SUMMARY.md`
</output>
