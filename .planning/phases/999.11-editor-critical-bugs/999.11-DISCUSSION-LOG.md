# Phase 999.11: Editor Critical Bugs - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-06
**Phase:** 999.11-editor-critical-bugs
**Areas discussed:** Playhead & sync strategy, Subtitle text safety, Trim & cascade math, Resource lifecycle, Testing & structure

---

## Playhead & Sync Strategy

### Q1: Which single source should drive the playhead frame?

| Option | Description | Selected |
|--------|-------------|----------|
| RAF loop only | Keep requestAnimationFrame in RemotionPreview (smooth 60fps). Remove timeupdate from Timeline. | ✓ |
| timeupdate event only | Use Remotion's built-in event. Lower frequency but event-driven. | |
| You decide | Claude picks based on Remotion Player API behavior. | |

**User's choice:** RAF loop only (Recommended)
**Notes:** None

### Q2: freezeFrame cascade behavior

| Option | Description | Selected |
|--------|-------------|----------|
| Cascade like trimScene | Add shiftSubtitles + shiftAudio after freeze, same pattern as trimScene. | ✓ |
| Freeze in-place (no shift) | Keep current behavior — frozen frames overlap with next content. | |
| You decide | Claude picks based on consistency. | |

**User's choice:** Cascade like trimScene (Recommended)
**Notes:** None

---

## Subtitle Text Safety

### Q3: How should inline subtitle editing prevent text loss?

| Option | Description | Selected |
|--------|-------------|----------|
| Local draft ref | useRef to hold draft while focused. Read from store on mount/selection, write on blur. | ✓ |
| Replace with textarea | Swap contentEditable for positioned textarea. Fully controlled. | |
| You decide | Claude picks preserving current visual feel. | |

**User's choice:** Local draft ref (Recommended)
**Notes:** None

### Q4: SRT fetch race condition prevention

| Option | Description | Selected |
|--------|-------------|----------|
| AbortController + generation counter | Pass AbortController to fetch, increment gen counter. Discard if changed. | ✓ |
| Move SRT fetch to useEffect in page | Extract async out of store action. Load from page component. | |
| You decide | Claude picks simplest approach. | |

**User's choice:** AbortController + generation counter (Recommended)
**Notes:** None

---

## Trim & Cascade Math

### Q5: Video left-trim handle behavior

| Option | Description | Selected |
|--------|-------------|----------|
| Add trimFrom to EditorScene | New field like audio's startFrom. Left handle adjusts both trimFrom and duration. | ✓ |
| Remove left handle for now | Right-trim only. Add proper left-trim in 999.12. | |
| You decide | Claude picks based on complexity vs value. | |

**User's choice:** Add trimFrom to EditorScene (Recommended)
**Notes:** None

---

## Resource Lifecycle

### Q6: AudioContext leak on zoom

| Option | Description | Selected |
|--------|-------------|----------|
| Singleton AudioContext + resample | One shared context. Cache by URL only. Resample peaks client-side on zoom. | ✓ |
| Pool with limit | 2-3 contexts reused round-robin. Still re-decodes. | |
| You decide | Claude picks for Safari 6-context limit. | |

**User's choice:** Singleton AudioContext + resample (Recommended)
**Notes:** None

---

## Testing & Structure (Revisited)

### Q7: Test coverage level

| Option | Description | Selected |
|--------|-------------|----------|
| Unit tests per fix | Each fix gets dedicated Vitest tests. Extend existing test file. | ✓ |
| Unit + integration | Unit + integration tests verifying fixes work together. | |
| Unit + integration + E2E | All above plus Playwright E2E. Full confidence, more work. | |

**User's choice:** Unit tests per fix (Recommended)
**Notes:** User emphasized: "every change need to be testable"

### Q8: Code structure for reusability

| Option | Description | Selected |
|--------|-------------|----------|
| Extract to lib/editor/ | Pure functions move to src/lib/editor/. Store imports from there. Clean separation. | ✓ |
| Keep in stores, just organize | Keep in editor-store.ts with clear sections. Less file churn. | |
| You decide | Claude structures for max testability and reuse. | |

**User's choice:** Extract to lib/editor/ (Recommended)
**Notes:** User emphasized: "the editor will be reused later, structure everything to be reusable clean and optimized"

---

## Claude's Discretion

- Internal file structure within `lib/editor/` (file count, naming)
- Test organization (one file per module vs grouped by feature)
- Whether to create barrel export (`lib/editor/index.ts`)

## Deferred Ideas

- Integration/E2E tests — deferred until editor stabilizes
- All UX enhancements — Phase 999.12
