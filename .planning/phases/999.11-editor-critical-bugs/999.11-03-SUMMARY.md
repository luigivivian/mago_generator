---
phase: 999.11-editor-critical-bugs
plan: 03
subsystem: ui
tags: [react, hooks, zustand, contenteditable, audiocontext, abortcontroller]

requires:
  - phase: 999.11-01
    provides: Pure editor functions (genId, parseSrt, shiftSubtitles) extracted to lib/editor/

provides:
  - Singleton AudioContext with URL-only rawCache and resamplePeaks
  - Autosave cleanup with AbortController + isMountedRef
  - SRT fetch race prevention via generation counter + AbortController
  - contentEditable draft isolation preventing text loss during typing
  - Live frame offset computation in context menu (no stale snapshots)

affects: [editor, timeline, subtitles, audio]

tech-stack:
  added: []
  patterns: [singleton-audiocontext, generation-counter-fetch, draft-ref-contenteditable, live-store-recompute]

key-files:
  created:
    - memelab/src/__tests__/lib-editor-waveform.test.ts
    - memelab/src/__tests__/use-autosave.test.ts
  modified:
    - memelab/src/hooks/use-audio-waveform.ts
    - memelab/src/hooks/use-autosave.ts
    - memelab/src/stores/editor-store.ts
    - memelab/src/components/editor/SubtitleEditor.tsx
    - memelab/src/components/editor/ContextMenu.tsx
    - memelab/src/components/editor/Timeline.tsx

key-decisions:
  - "Float32Array raw cache keyed by URL only -- zoom changes resample cheaply without re-decode"
  - "Generation counter + AbortController for SRT fetch race (both loadFromStepState and loadSubtitlesFromSrt)"
  - "D-01 already resolved: no timeupdate listener exists, RAF loop is sole playhead source"

patterns-established:
  - "Singleton AudioContext: single getAudioContext() function, never closed, shared across all waveform decodes"
  - "Generation counter fetch: increment counter before fetch, check thisGen === generation in .then() to discard stale responses"
  - "Draft ref for contentEditable: sync from store only when element is not focused (document.activeElement guard)"
  - "Live store recompute: context menu recalculates startFrame from live scenes array instead of using snapshot"

requirements-completed: []

duration: 6min
completed: 2026-04-06
---

# Phase 999.11 Plan 03: UI/Lifecycle Bugs Summary

**Fix 6 editor bugs: AudioContext singleton, autosave cleanup, SRT fetch race, contentEditable draft isolation, playhead dedup confirmation, and context menu live offset**

## Performance

- **Duration:** 6 min
- **Started:** 2026-04-06T19:50:48Z
- **Completed:** 2026-04-06T19:57:03Z
- **Tasks:** 2
- **Files modified:** 8

## Accomplishments
- AudioContext is now a singleton with URL-only rawCache; zoom changes resample without re-decode (D-08)
- Autosave properly cancels in-flight requests and timers on unmount via AbortController + isMountedRef (D-09)
- SRT fetch uses generation counter + AbortController to prevent stale responses from overwriting subtitles (D-04)
- contentEditable text preserved during typing via draftRef + editRef with activeElement focus guard (D-03)
- Confirmed no duplicate timeupdate listener exists; RAF loop is sole playhead source (D-01)
- Context menu computes liveStartFrame from store scenes array, not stale snapshot (D-10)

## Task Commits

Each task was committed atomically:

1. **Task 1: Fix AudioContext leak, autosave cleanup, and SRT fetch race** - `eb20197` (fix)
2. **Task 2: Fix contentEditable draft, playhead dedup, and context menu stale offset** - `e2440af` (fix)

## Files Created/Modified
- `memelab/src/hooks/use-audio-waveform.ts` - Singleton AudioContext + rawCache + resamplePeaks export
- `memelab/src/hooks/use-autosave.ts` - AbortController + isMountedRef + idleTimerRef cleanup
- `memelab/src/stores/editor-store.ts` - srtGeneration counter + AbortController for both SRT fetch paths
- `memelab/src/components/editor/SubtitleEditor.tsx` - draftRef + editRef with focus guard for contentEditable
- `memelab/src/components/editor/ContextMenu.tsx` - liveStartFrame + liveDurationFrames from store scenes
- `memelab/src/components/editor/Timeline.tsx` - D-01 documentation comment (no code change needed)
- `memelab/src/__tests__/lib-editor-waveform.test.ts` - resamplePeaks correctness tests (5 cases)
- `memelab/src/__tests__/use-autosave.test.ts` - useAutosave module export/compile check

## Decisions Made
- D-01 (duplicate playhead sync) was already resolved: no timeupdate listener exists anywhere in the codebase. Added documentation comment only.
- Used Float32Array for raw peaks cache to minimize memory while supporting arbitrary resample counts.
- Applied generation counter pattern to both loadFromStepState and loadSubtitlesFromSrt for consistency.
- Used toBeCloseTo instead of toBe in waveform tests due to Float32Array precision.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Float32Array precision in tests**
- **Found during:** Task 1 (resamplePeaks tests)
- **Issue:** Float32Array stores values at 32-bit precision, so `0.8` becomes `0.800000011920929`
- **Fix:** Changed `toBe(0.8)` to `toBeCloseTo(0.8, 5)` in test assertions
- **Files modified:** memelab/src/__tests__/lib-editor-waveform.test.ts
- **Verification:** All 5 tests pass
- **Committed in:** eb20197

---

**Total deviations:** 1 auto-fixed (1 bug)
**Impact on plan:** Test precision fix only, no functional change.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- All 6 UI/lifecycle bugs fixed (D-01, D-03, D-04, D-08, D-09, D-10)
- Full test suite passes (43 tests, 0 failures)
- Editor hooks and components are now resource-leak-free and race-condition-safe

## Self-Check: PASSED

All 8 created/modified files verified on disk. Both task commits (eb20197, e2440af) found in git log.

---
*Phase: 999.11-editor-critical-bugs*
*Completed: 2026-04-06*
