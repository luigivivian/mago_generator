---
phase: 25-structured-image-generation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_reels_image_gen.py
  - tests/conftest.py
autonomous: true
requirements:
  - IMAGE-01
  - IMAGE-02
  - IMAGE-03
  - IMAGE-04

must_haves:
  truths:
    - "4 xfail test stubs exist, each mapped to one IMAGE requirement"
    - "A fake_gemini_image_client fixture exists that returns image data (not PCM TTS data)"
    - "Running pytest on the test file produces 4 xfail results with no errors"
  artifacts:
    - path: "tests/test_reels_image_gen.py"
      provides: "xfail stubs for IMAGE-01..IMAGE-04"
      contains: "xfail"
    - path: "tests/conftest.py"
      provides: "fake_gemini_image_client fixture"
      contains: "fake_gemini_image_client"
  key_links:
    - from: "tests/test_reels_image_gen.py"
      to: "tests/conftest.py"
      via: "fake_gemini_image_client fixture import"
      pattern: "fake_gemini_image_client"
---

<objective>
Wave 0: Create xfail test stubs for IMAGE-01 through IMAGE-04 and a fake Gemini image client fixture.

Purpose: Establish the test scaffold BEFORE any production code changes, following the xfail pattern from Phases 22/23/24. Plan 02 will flip these stubs to active GREEN as it implements each requirement.
Output: `tests/test_reels_image_gen.py` with 4 xfail tests, `tests/conftest.py` extended with `fake_gemini_image_client` fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-structured-image-generation/25-RESEARCH.md

@tests/conftest.py
@src/reels_pipeline/image_gen.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add fake_gemini_image_client fixture to conftest.py</name>
  <files>tests/conftest.py</files>
  <read_first>tests/conftest.py, src/reels_pipeline/image_gen.py</read_first>
  <action>
Add a new class `FakeGeminiImageResponse` and a new fixture `fake_gemini_image_client` to `tests/conftest.py`.

The existing `FakeGeminiResponse` returns PCM bytes for TTS. Image generation needs a response where `part.inline_data.mime_type.startswith("image/")` is True and `part.inline_data.data` is bytes (see `image_gen.py:346-348`).

1. Create `FakeGeminiImageResponse` class:
   - Constructor takes no args (or optional bytes)
   - Creates a part with `inline_data.mime_type = "image/jpeg"` and `inline_data.data = <1x1 JPEG bytes>`
   - Use a minimal valid JPEG: `b'\xff\xd8\xff\xe0\x00\x10JFIF\x00\x01\x01\x00\x00\x01\x00\x01\x00\x00\xff\xdb\x00C\x00\x08\x06\x06\x07\x06\x05\x08\x07\x07\x07\t\t\x08\n\x0c\x14\r\x0c\x0b\x0b\x0c\x19\x12\x13\x0f\x14\x1d\x1a\x1f\x1e\x1d\x1a\x1c\x1c $.\' ",#\x1c\x1c(7),01444\x1f\'9=82<.342\xff\xc0\x00\x0b\x08\x00\x01\x00\x01\x01\x01\x11\x00\xff\xc4\x00\x1f\x00\x00\x01\x05\x01\x01\x01\x01\x01\x01\x00\x00\x00\x00\x00\x00\x00\x00\x01\x02\x03\x04\x05\x06\x07\x08\t\n\x0b\xff\xc4\x00\xb5\x10\x00\x02\x01\x03\x03\x02\x04\x03\x05\x05\x04\x04\x00\x00\x01}\x01\x02\x03\x00\x04\x11\x05\x12!1A\x06\x13Qa\x07"q\x142\x81\x91\xa1\x08#B\xb1\xc1\x15R\xd1\xf0$3br\x82\t\n\x16\x17\x18\x19\x1a%&\'()*456789:CDEFGHIJSTUVWXYZcdefghijstuvwxyz?\x00\xfb\xd2\x8a+\xff\xd9'`
   - Actually simpler approach: generate a 1x1 red pixel JPEG via PIL in the fixture setup, store as bytes. That is more reliable than hardcoded bytes.

2. Create `FakeGeminiImageClient` class extending the pattern from `FakeGeminiClient`:
   - `__init__`: `self.calls = []`, `self.models = self`
   - `generate_content(*, model, contents, config)`: appends call dict to `self.calls`, returns `FakeGeminiImageResponse`
   - The call dict must capture: `model`, `contents`, `config` (same as FakeGeminiClient)

3. Create fixture `fake_gemini_image_client(monkeypatch)`:
   - Instantiate `FakeGeminiImageClient`
   - Monkeypatch BOTH targets (from-import binding pattern):
     - `monkeypatch.setattr("src.llm_client._get_client", lambda: fake)`
     - `monkeypatch.setattr("src.reels_pipeline.image_gen._get_client", lambda: fake)`
   - Return the fake client

Do NOT modify any existing fixtures or classes.
  </action>
  <verify>
    <automated>cd /Users/luigivivian/meme-lab && python -c "from tests.conftest import FakeGeminiImageClient; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fake_gemini_image_client" tests/conftest.py` returns >= 2 (class + fixture)
    - `grep -c "FakeGeminiImageResponse" tests/conftest.py` returns >= 1
    - `grep "image_gen._get_client" tests/conftest.py` returns a match (monkeypatch target)
  </acceptance_criteria>
  <done>FakeGeminiImageClient and fake_gemini_image_client fixture exist in conftest.py, importable without errors</done>
</task>

<task type="auto">
  <name>Task 2: Create xfail test stubs for IMAGE-01..IMAGE-04</name>
  <files>tests/test_reels_image_gen.py</files>
  <read_first>tests/test_reels_script_schema.py, tests/conftest.py, src/reels_pipeline/image_gen.py</read_first>
  <action>
Create `tests/test_reels_image_gen.py` with 4 xfail/strict test stubs following the Phase 24 pattern (see `test_reels_script_schema.py` for format).

File header:
```python
"""Phase 25: Structured Image Generation -- validation suite.

Wave 0 (plan 25-01) creates all 4 tests as xfail stubs.
Wave 1 (plan 25-02) flips all 4 tests from xfail -> active.

DO NOT rename a test without updating 25-VALIDATION.md.
"""

from __future__ import annotations

import pytest
```

Test stubs (each marked `@pytest.mark.xfail(strict=True, reason="Wave 0 stub — Plan 25-02 implements")`):

1. `test_01_image_prompt_primary` (IMAGE-01):
   - Calls `generate_reel_images_per_cena` with a cena that has `image_prompt="A wizard in a forest, lush greenery, cartoon style, wide angle, 9:16, no text, no watermark"`
   - Asserts the captured prompt (from `fake_gemini_image_client.calls[0]["contents"][-1]`) contains the `image_prompt` text
   - Asserts the captured prompt does NOT contain `narracao` text (proving image_prompt is primary, not narracao)
   - Stub body: `pytest.fail("Not implemented — Plan 25-02")`

2. `test_02_style_seed_prepended` (IMAGE-02):
   - Calls `generate_reel_images_per_cena` with a cena with `image_prompt` AND passes `character_card={"description": "Old wizard", "style_seed": "Soft cel-shading cartoon, blue robe wizard"}`
   - Asserts `"Soft cel-shading cartoon, blue robe wizard"` appears in captured prompt
   - Asserts style_seed appears BEFORE image_prompt in the prompt string
   - Stub body: `pytest.fail("Not implemented — Plan 25-02")`

3. `test_03_bible_style_combined` (IMAGE-03):
   - Calls `generate_reel_images_per_cena` with `config_override={"bible_config": {"book": "Genesis"}}` and a cena with `image_prompt`
   - Asserts captured prompt contains BOTH `BIBLE_STYLE_DNA` substring (e.g., "Bible Project") AND the `image_prompt` text
   - Stub body: `pytest.fail("Not implemented — Plan 25-02")`

4. `test_04_aspect_ratio_explicit` (IMAGE-04):
   - Calls `generate_reel_images_per_cena` with a cena with `image_prompt` (that does NOT contain "9:16")
   - Asserts captured prompt contains "9:16"
   - Stub body: `pytest.fail("Not implemented — Plan 25-02")`

Each test function signature: `async def test_XX_name(fake_gemini_image_client, tmp_path):`
Mark all tests with `@pytest.mark.asyncio` since `generate_reel_images_per_cena` is async.

All 4 tests should be `xfail(strict=True)` meaning they MUST fail in Wave 0. Each has a section comment header with requirement ID and bound plan (same format as test_reels_script_schema.py).
  </action>
  <verify>
    <automated>cd /Users/luigivivian/meme-lab && python -m pytest tests/test_reels_image_gen.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "xfail" tests/test_reels_image_gen.py` returns >= 4
    - `grep -c "def test_" tests/test_reels_image_gen.py` returns exactly 4
    - `grep "IMAGE-01" tests/test_reels_image_gen.py` returns a match
    - `grep "IMAGE-04" tests/test_reels_image_gen.py` returns a match
    - pytest output shows "4 xfailed" with 0 errors
  </acceptance_criteria>
  <done>4 xfail test stubs exist in test_reels_image_gen.py, all passing as xfail (expected failures), 0 errors</done>
</task>

</tasks>

<verification>
cd /Users/luigivivian/meme-lab && python -m pytest tests/test_reels_image_gen.py -v 2>&1 | grep -E "XFAIL|PASSED|FAILED|ERROR"
# Expected: 4 XFAIL, 0 PASSED, 0 FAILED, 0 ERROR
</verification>

<success_criteria>
- tests/test_reels_image_gen.py exists with 4 xfail stubs (test_01..test_04)
- tests/conftest.py has fake_gemini_image_client fixture with dual monkeypatch
- `python -m pytest tests/test_reels_image_gen.py -x -q` runs clean (4 xfailed)
- Full suite `python -m pytest tests/ -x -q` still passes (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/25-structured-image-generation/25-01-SUMMARY.md`
</output>
